export interface User {
  userId: number; // Use number type for userId
  userName: string; // Use string type for userName
  userMail: string; // Use string type for userMail
  userPassword: string; // Use string type for userPassword
  isDeleted: boolean;
  userRoleId: number; // Use number type for userRoleId
   
}